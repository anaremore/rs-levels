name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  verify:
    name: Verify public package (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'

      - name: Test
        run: npm test

      - name: Private-boundary scan
        run: npm run scan:private

      - name: Credential pattern scan
        run: npm run scan:secrets

      - name: Build release directory
        run: npm run package

      - name: Upload release directory
        uses: actions/upload-artifact@v6
        with:
          name: rs-levels-source-package-${{ matrix.os }}
          path: dist/rs-levels-0.0.0
          if-no-files-found: error
